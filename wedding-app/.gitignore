# The repository root .gitignore ignores *.css (it targets Metaplex build output).
# This wedding app ships hand-written CSS that MUST be versioned — re-include it.
!css/styles.css
!**/*.css

# Build output (regenerated by `npm run build`) and installed deps
dist/
node_modules/
